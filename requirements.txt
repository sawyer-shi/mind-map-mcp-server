# Mind Map MCP Server Python Dependencies | 思维导图MCP服务器的Python依赖
# =====================================================

# MCP protocol support with multiple transports | MCP协议支持（包含多种传输方式）
# Includes stdio and streamable-http transports | 包含stdio和streamable-http传输
mcp[cli]>=1.12.0

# Browser automation for HTML to PNG conversion | 浏览器自动化工具（HTML转PNG）
playwright>=1.40.0

# Web server support for HTTP transports | Web服务器支持（用于HTTP传输）
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Additional MCP transport dependencies | 额外的MCP传输依赖
# WebSocket support for advanced MCP features | WebSocket支持（用于高级MCP功能）
websockets>=12.0

# Type checking and validation | 类型检查和验证
pydantic>=2.0.0
typing-extensions>=4.8.0

# Configuration management | 配置管理
python-dotenv>=1.0.0

# JSON处理（Python内置，这里列出来让你知道会用到）
# json - 内置模块

# 系统进程管理（Python内置）
# subprocess - 内置模块

# 时间处理（Python内置）
# time - 内置模块

# Base64编码（Python内置）
# base64 - 内置模块

# 操作系统接口（Python内置）
# os - 内置模块

# 系统特定参数（Python内置）
# sys - 内置模块

# 临时文件处理（Python内置）
# tempfile - 内置模块

# 类型注解支持（Python内置）
# typing - 内置模块